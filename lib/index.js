'use strict';

// MODULES //




// MAIN //

/**
* FUNCTION: mdeqn( options, clbk )
*	Converts a TeX/LaTeX string to an SVG and generates an HTML string for inclusion in a Markdown file.
*
* @param {Object} options - function options
* @param {Function} clbk - callback to invoke after completion
* @returns {Void}
*/
function mdeqn( options, clbk ) {

} // end FUNCTION mdeqn()


// EXPORTS //

module.exports = mdeqn;
